#1. Write a Python program to create a list and add all the items in a list.
# l = []
# n = int(input())
# for i in range(n):
#     n = input()
#     l.append(n)
# print(l)

#2. Write a Python program to reverse the list.
# l = [1,2,3,4,5]
# print(l[::-1])
# l.reverse()
# print(l)

#3. c.Write a program to add two lists index-wise. Create a new list that contains the 0th index item from both the list, then the 1st index item, and so on till the last element. any leftover items will get added at the end of the new .
# L1 = ["M", "NA"]
# L2 = ["Y", "ME"]
# result_list = []
# length = len(L1)
# for i in range(length):
#     result_list.append(L1[i] + L2[i])
# print("Output:", result_list)

#4. Write a Python program to print all the elements at odd position and take a list as input through cmd.
#l = []
#j = []
#n = int(input("Enter the amount of numbers: "))
#for i in range(n):
 #   m = int(input("Enter your number: "))
 #   l.append(m)
#    if i%2 != 0:
#        j.append(l[i])
#print("Elements at odd position in the list are:", j)
#print("Given list are:",l)